import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AI Vocabulary Trainer - Learn IT Terms | 2026',
  description: 'AI-powered English vocabulary trainer for IT professionals. Learn network, coding, and hardware terms with interactive quizzes and progress tracking.',
  keywords: ['vocabulary', 'english', 'IT', 'learning', 'AI', '2026', 'technology'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
}
